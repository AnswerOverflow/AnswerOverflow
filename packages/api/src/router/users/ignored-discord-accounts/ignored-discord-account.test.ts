import { clearDatabase } from "@answeroverflow/db";

beforeEach(async () => {
  await clearDatabase();
});

describe("Ignored Discord Account Operations", () => {
  it("should create an ignored discord account", () => {});
});
