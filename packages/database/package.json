{
	"name": "@packages/database",
	"version": "1.0.0",
	"exports": {
		"./*": "./src/*.ts",
		"./convex/shared/*": "./convex/shared/*.ts",
		"./convex/*": {
			"types": "./convex/*.d.js",
			"default": "./convex/*.js"
		}
	},
	"scripts": {
		"with-env": "bun run --env-file=../../.env",
		"codegen": "bunx convex codegen && bun run scripts/generate-function-types.ts",
		"generate-function-types": "bun run scripts/generate-function-types.ts",
		"dev": "convex dev",
		"typecheck": "tsc -p convex/tsconfig.json --noEmit & tsc -p tsconfig.json --noEmit & wait",
		"clean": "rm -rf node_modules .turbo",
		"test": "vitest run --no-watch --pool=forks",
		"test:watch": "vitest",
		"convex:knip": "bun scripts/find-unused-convex-functions.ts"
	},
	"dependencies": {
		"@convex-dev/action-cache": "^0.3.0",
		"@convex-dev/better-auth": "catalog:",
		"@effect/platform": "catalog:",
		"@packages/analytics": "workspace:*",
		"@packages/discord-api": "workspace:*",
		"convex": "catalog:",
		"convex-helpers": "^0.1.104",
		"effect": "catalog:"
	},
	"devDependencies": {
		"@edge-runtime/vm": "^5.0.0",
		"@effect/vitest": "^0.25.0",
		"@packages/convex-test": "workspace:*",
		"@packages/observability": "workspace:*",
		"glob": "^11.0.3",
		"vitest": "catalog:"
	}
}
