---
interface Props {
	title: string;
}

const { title } = Astro.props;
import { Navbar } from '@answeroverflow/ui/src/navbar/index.tsx';
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="dark">
		<Navbar tenant={undefined} />
		<!-- {
			latestChangelog &&
				latestChangelog.showInBanner &&
				latestChangelog.bannerText && (
					<div
						className={
							'bg-primary/5 hidden w-full items-center justify-center py-1 md:flex'
						}
					>
						<span className={'text-primary text-center text-sm'}>
							{latestChangelog.bannerText}
						</span>
					</div>
				)
		} -->
		<slot />
	</body>
</html>
