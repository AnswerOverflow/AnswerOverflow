{
	"name": "@apps/bot-e2e",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"scripts": {
		"with-env": "bun run --env-file=../../.env.production",
		"test": "bun run with-env vitest run --no-watch",
		"test:watch": "bun run with-env vitest",
		"test:mark-solution": "bun run with-env vitest run tests/mark-solution.test.ts",
		"setup": "bun run with-env bun run src/scripts/setup-server.ts",
		"wipe": "bun run with-env bun run src/scripts/wipe-server.ts",
		"teardown": "bun run with-env bun run src/scripts/teardown-server.ts",
		"typecheck": "tsgo -p tsconfig.json --noEmit",
		"typecheck:slow": "tsc -p tsconfig.json --noEmit"
	},
	"dependencies": {
		"@effect/platform": "^0.93.8",
		"@packages/database": "workspace:*",
		"discord.js-selfbot-v13": "^3.7.1",
		"effect": "catalog:"
	},
	"devDependencies": {
		"@effect/vitest": "catalog:",
		"@packages/typescript-config": "workspace:*",
		"@types/bun": "latest",
		"vitest": "catalog:"
	}
}
