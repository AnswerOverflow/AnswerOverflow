describe("Mark Solution Tests", () => {
  describe("Check if can mark solution", () => {
    it("should fail without a guild", async () => {});
    it("should fail if the message is not in a thread", async () => {});
    it("should fail if the message is from Answer Overflow Bot", async () => {});
  });
});
