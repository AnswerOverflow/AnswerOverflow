{
	"buildCommand": "timeout 300 bash -c 'export NEXT_PUBLIC_ANALYTICS_PATH=$(openssl rand -hex 4) && cd ../../packages/database && npx convex deploy --cmd-url-env-var-name NEXT_PUBLIC_CONVEX_URL && cd ../../apps/main-site && bun run build'"
}
