# Channel Settings

These settings apply only to the current channel. They are accessed via the `/channel-settings` slash command.

import { Callout } from 'nextra-theme-docs';

<Callout type="info">
	Trying to change the settings of a forum channel? You can either select the
	forum channel in the `/channel-settings` slash command, or use this command in
	a thread of that channel!
</Callout>

{/* Link directory to all channel settings pages? */}
