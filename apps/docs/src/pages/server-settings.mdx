# Server Settings

These are global settings that apply to your entire server accessed from the `/server-settings` slash command.

{/* TODO: Server settings screenshot? */}
{/* Link directory to all server settings pages? */}
